# ! usually designates that a method will be a mutator and change the calling or parameter object
# ? usually designates that a method will return a boolean value

# 1. != is not equals and should be used in comparison conditionals
# 2. !user_name would return the boolean value opposite of user_name
# 3. words.uniq! would call the uniq method which mutates the calling object, words
# 4. ? before something is the ternary operator
# 5. ? after something like a method will return the boolean value of whatever the method is checking
# 6. !! before something gives the boolean of the truthy value of user_name